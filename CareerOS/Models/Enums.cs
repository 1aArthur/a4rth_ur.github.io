namespace CareerOS.Models;

public enum StudyStatus { NotStarted, InProgress, Completed }
public enum PriorityLevel { Low, Medium, High }
public enum SessionType { Reading, Exercises, Review, Project }
public enum ExerciseStatus { Pending, Solved, ReviewNeeded }
