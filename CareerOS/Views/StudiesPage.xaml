<Page x:Class="CareerOS.Views.StudiesPage" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Padding="20" RowSpacing="10">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
        <TextBlock Text="Matérias e Trilhas" FontSize="28" FontWeight="Bold"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="8">
            <TextBox Width="220" PlaceholderText="Matéria" Text="{Binding Name, Mode=TwoWay}"/>
            <TextBox Width="260" PlaceholderText="Descrição" Text="{Binding Description, Mode=TwoWay}"/>
            <Button Content="Adicionar" Command="{Binding AddSubjectCommand}"/>
        </StackPanel>
        <ListView Grid.Row="2" ItemsSource="{Binding Subjects}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Background="#1E1E38" CornerRadius="10" Margin="0,4" Padding="12">
                        <StackPanel>
                            <TextBlock Text="{Binding Name}" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding Description}" Opacity="0.8"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
